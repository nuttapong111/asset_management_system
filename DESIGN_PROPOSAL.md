# แนวทางการจัดการทรัพย์สินแบบ Hierarchical (ที่ดิน → ห้องเช่า)

## 📋 สถานการณ์
- เริ่มต้น: ที่ดินเปล่า 1 แปลง
- พัฒนา: สร้างเป็นห้องเช่า 10 ห้องในแปลงเดียวกัน

## 🎯 แนวทางที่แนะนำ: Hierarchical Asset Structure

### โครงสร้างข้อมูล

```typescript
export interface Asset {
  id: string;
  ownerId: string;
  type: AssetType; // 'land' → 'apartment' (เมื่อพัฒนาแล้ว)
  name: string;
  address: string;
  // ... ข้อมูลพื้นฐานอื่นๆ
  
  // เพิ่มฟิลด์ใหม่สำหรับ Hierarchical Structure
  parentAssetId?: string;        // ID ของที่ดินหลัก (ถ้าเป็นห้องเช่า)
  isParent: boolean;            // true = ที่ดินหลัก, false = ห้องเช่า
  childAssets?: string[];        // Array of child asset IDs (ถ้าเป็นที่ดินหลัก)
  unitNumber?: string;            // เลขห้อง (เช่น "101", "102") สำหรับห้องเช่า
  totalUnits?: number;           // จำนวนห้องทั้งหมด (สำหรับที่ดินหลัก)
  
  // ข้อมูลการพัฒนา
  developmentHistory?: {
    date: string;
    action: 'land_purchased' | 'construction_started' | 'construction_completed' | 'units_created';
    description: string;
  }[];
}
```

### ตัวอย่างข้อมูล

**1. ที่ดินหลัก (Parent Asset)**
```json
{
  "id": "asset_001",
  "type": "land",
  "name": "ที่ดินแปลง A",
  "isParent": true,
  "totalUnits": 10,
  "childAssets": ["asset_002", "asset_003", ..., "asset_011"],
  "size": 500,  // ตร.ม. ทั้งแปลง
  "status": "rented"  // ถ้ามีห้องเช่าบางห้อง
}
```

**2. ห้องเช่าแต่ละห้อง (Child Assets)**
```json
{
  "id": "asset_002",
  "type": "apartment",
  "name": "ห้อง 101 - ที่ดินแปลง A",
  "parentAssetId": "asset_001",
  "isParent": false,
  "unitNumber": "101",
  "size": 30,  // ตร.ม. ของห้องนี้
  "rooms": 1,
  "status": "rented",
  "latitude": 13.7563,  // ใช้ตำแหน่งเดียวกับที่ดินหลัก
  "longitude": 100.5018
}
```

## 🔄 Workflow การพัฒนา

### ขั้นตอนที่ 1: สร้างที่ดินเปล่า
1. เพิ่มทรัพย์สินใหม่ → เลือก type = "land"
2. กรอกข้อมูลที่ดิน (ขนาด, ราคาซื้อ, ตำแหน่ง)
3. `isParent = true`, `totalUnits = 0`

### ขั้นตอนที่ 2: เริ่มก่อสร้าง
1. แก้ไขทรัพย์สิน → เพิ่ม development history
2. `action: 'construction_started'`
3. อาจเปลี่ยน `status = 'maintenance'`

### ขั้นตอนที่ 3: สร้างห้องเช่า 10 ห้อง
1. ฟังก์ชัน "สร้างห้องเช่าจากที่ดิน"
2. ระบบสร้าง asset ใหม่ 10 รายการ:
   - `parentAssetId = asset_001`
   - `isParent = false`
   - `unitNumber = "101", "102", ..., "110"`
   - ใช้ตำแหน่งเดียวกับที่ดินหลัก
3. อัปเดตที่ดินหลัก:
   - `totalUnits = 10`
   - `childAssets = [asset_002, ..., asset_011]`
   - `type` อาจเปลี่ยนเป็น `'apartment'` หรือเก็บเป็น `'land'` ก็ได้

## 📊 การจัดการสัญญาเช่า

### แต่ละห้องเช่ามีสัญญาแยกกัน
```typescript
// ห้อง 101
{
  "id": "contract_001",
  "assetId": "asset_002",  // ห้อง 101
  "rentAmount": 5000
}

// ห้อง 102
{
  "id": "contract_002",
  "assetId": "asset_003",  // ห้อง 102
  "rentAmount": 5000
}
```

## 💰 การรายงานและการเงิน

### 1. รายได้รายห้อง
- แต่ละห้องมีสัญญาและรายได้แยกกัน
- สามารถดูรายได้ของแต่ละห้องได้

### 2. รายได้รวมของที่ดิน
```typescript
function getTotalIncome(parentAssetId: string) {
  const parentAsset = getAsset(parentAssetId);
  const childAssets = parentAsset.childAssets.map(id => getAsset(id));
  const contracts = childAssets.flatMap(asset => 
    getContractsByAsset(asset.id)
  );
  return contracts.reduce((sum, contract) => sum + contract.rentAmount, 0);
}
```

### 3. อัตราการเช่า (Occupancy Rate)
```typescript
function getOccupancyRate(parentAssetId: string) {
  const parentAsset = getAsset(parentAssetId);
  const childAssets = parentAsset.childAssets.map(id => getAsset(id));
  const rentedCount = childAssets.filter(a => a.status === 'rented').length;
  return (rentedCount / parentAsset.totalUnits) * 100;
}
```

## 🎨 UI/UX ที่แนะนำ

### 1. หน้า Dashboard
- แสดงที่ดินหลักเป็น marker ใหญ่
- เมื่อคลิก → แสดง sidebar พร้อมรายการห้องเช่าทั้ง 10 ห้อง
- แสดงสถานะของแต่ละห้อง (ว่าง/เช่าแล้ว/ซ่อม)

### 2. หน้ารายละเอียดทรัพย์สิน
- **Tab: ภาพรวม** - ข้อมูลที่ดินหลัก
- **Tab: ห้องเช่า** - แสดงรายการห้องเช่าทั้งหมด พร้อมสถานะ
- **Tab: การเช่า** - สัญญาเช่าของทุกห้อง
- **Tab: การเงิน** - รายได้รวมและรายได้รายห้อง
- **Tab: การพัฒนา** - ประวัติการพัฒนาที่ดิน

### 3. ฟังก์ชัน "สร้างห้องเช่า"
- ปุ่ม "สร้างห้องเช่าจากที่ดิน"
- ฟอร์ม:
  - จำนวนห้อง (10)
  - ขนาดห้อง (ตร.ม.)
  - จำนวนห้องนอน
  - ราคาเช่าต่อเดือน
- ระบบสร้าง asset ใหม่ 10 รายการอัตโนมัติ

## 🔧 ฟังก์ชันที่ต้องเพิ่ม

### 1. `createUnitsFromParent(parentAssetId, unitConfig)`
```typescript
function createUnitsFromParent(
  parentAssetId: string,
  config: {
    numberOfUnits: number;
    unitSize: number;
    rooms: number;
    baseRentAmount?: number;
  }
): Asset[] {
  // สร้าง asset ใหม่สำหรับแต่ละห้อง
  // เชื่อมโยงกับ parentAssetId
  // อัปเดต parent asset
}
```

### 2. `getChildAssets(parentAssetId: string): Asset[]`
```typescript
function getChildAssets(parentAssetId: string): Asset[] {
  return mockAssets.filter(a => a.parentAssetId === parentAssetId);
}
```

### 3. `getParentAsset(childAssetId: string): Asset | null`
```typescript
function getParentAsset(childAssetId: string): Asset | null {
  const child = mockAssets.find(a => a.id === childAssetId);
  if (!child?.parentAssetId) return null;
  return mockAssets.find(a => a.id === child.parentAssetId) || null;
}
```

## 📈 ข้อดีของแนวทางนี้

1. ✅ **ยืดหยุ่น** - รองรับทั้งทรัพย์สินเดี่ยวและทรัพย์สินที่มีหน่วยย่อย
2. ✅ **ง่ายต่อการจัดการ** - แต่ละห้องเช่ามีข้อมูลแยกกัน
3. ✅ **รองรับการเปลี่ยนแปลง** - สามารถเพิ่ม/ลบห้องเช่าได้
4. ✅ **การรายงานครบถ้วน** - ดูรายได้รายห้องหรือรวมทั้งหมด
5. ✅ **สัญญาเช่าแยกกัน** - แต่ละห้องมีสัญญาและผู้เช่าแยกกัน
6. ✅ **ประวัติการพัฒนา** - เก็บประวัติการพัฒนาที่ดิน

## 🚀 ขั้นตอนการ Implement

1. **Phase 1: อัปเดต Data Model**
   - เพิ่มฟิลด์ `parentAssetId`, `isParent`, `unitNumber`, `totalUnits`, `childAssets`
   - อัปเดต TypeScript types

2. **Phase 2: ฟังก์ชันจัดการ**
   - `createUnitsFromParent()`
   - `getChildAssets()`
   - `getParentAsset()`

3. **Phase 3: UI Components**
   - แสดง hierarchical structure ใน dashboard
   - ฟอร์มสร้างห้องเช่า
   - Tab "ห้องเช่า" ในหน้ารายละเอียด

4. **Phase 4: Reporting**
   - รายได้รวมของที่ดิน
   - อัตราการเช่า
   - รายได้รายห้อง

## 💡 ทางเลือกอื่น (ไม่แนะนำ)

### ทางเลือก 1: Asset Units Array
```typescript
interface Asset {
  units: {
    unitNumber: string;
    size: number;
    status: AssetStatus;
    contractId?: string;
  }[];
}
```
**ข้อเสีย:** ยากต่อการจัดการสัญญาและการเงินแยกกัน

### ทางเลือก 2: แยก Asset แต่อ้างอิงกัน
```typescript
interface Asset {
  relatedAssetIds?: string[];  // อ้างอิงกันแบบ loose
}
```
**ข้อเสีย:** ไม่ชัดเจนว่าใครเป็น parent ใครเป็น child

## ✅ สรุป

**แนะนำใช้ Hierarchical Structure** เพราะ:
- ชัดเจนว่าใครเป็น parent ใครเป็น child
- ง่ายต่อการจัดการและรายงาน
- รองรับการเปลี่ยนแปลงในอนาคต
- แต่ละห้องเช่ามีข้อมูลและสัญญาแยกกัน

